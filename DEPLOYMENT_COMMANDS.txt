# Commands to run in PowerShell/Command Prompt
# Navigate to your project folder first:
cd "c:\Users\armst\Downloads\Ikonne\Front"

# Initialize git repository
git init

# Add all files
git add .

# Make first commit
git commit -m "Initial commit: Portfolio website with React and Vite"

# Add your GitHub repository as remote (replace YOUR_USERNAME with your GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/ikonne-portfolio.git

# Push to GitHub
git branch -M main
git push -u origin main